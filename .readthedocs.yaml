version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"

python:
  install:
    - method: pip
      path: .
    - requirements: requirements.txt
    - pip:
        - git+https://github.com/lassemadsen/surface_plot#egg=surface_plot
